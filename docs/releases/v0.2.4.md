# v0.2.4 - 2026-09-19

**Status:** Managed Containment Bindings

v0.2.4 adds managed contained-process handles for Go and TypeScript. Each
handle owns the native child lifecycle and exposes identity, polling, bounded
waiting, termination, and deterministic close operations.

On Unix, successful managed spawn reports `guaranteed` race-free acquisition
and retained group-signaling eligibility with `cooperative_group` boundary
strength. Descendants can leave a cooperative process group, so this does not
claim OS-enforced non-escape. Windows rejects managed spawn before executing
the requested program.

## Highlights

- **Managed Go lifecycle**: `SpawnContained` returns a `ContainedProcess` whose
  `Identity`, `Poll`, `Wait`, `Terminate`, and `Close` methods operate through
  an opaque native-owned capability.
- **Managed TypeScript lifecycle**: `spawnContained` returns a disposable
  `ContainedProcess` with async `identity`, `poll`, `wait`, `terminate`, and
  `close` methods.
- **Native execution deadlines**: optional deadlines are enforced by the native
  owner without language-side polling. Cleanup retries preserve the deadline
  until a terminal transition is confirmed.
- **Explicit completion evidence**: natural completion remains distinct from
  deadline enforcement, acquisition reliability, and boundary strength.
- **Fail-closed lifecycle cleanup**: active handles retain their capability
  until cleanup confirms that the child has been reaped, allowing failed close
  attempts to be retried safely.
- **Release integrity checks**: machine-readable surface plans and guards bind
  release provenance, tag coordinates, generated bindings, downloaded assets,
  checksums, signatures, remote draft contents, and publication state.

## Release Coordinates

The release pack is merged before tags are created. Publication workflows run
from the verified tag-target commit after the Go prebuilt libraries are
generated and merged.

| Surface | v0.2.4 coordinate |
| --- | --- |
| Rust crates | `sysprims-core`, `sysprims-signal`, `sysprims-session`, `sysprims-proc`, `sysprims-timeout` at `0.2.4` |
| Rust workspace | Repository tag `v0.2.4` |
| Go module | Path-prefixed tag `bindings/go/sysprims/v0.2.4` on the same commit |
| TypeScript | `@3leaps/sysprims@0.2.4`, published from the verified `v0.2.4` tag |
| PTY adapter | Separate `sysprims-pty` release lifecycle using the core revision recorded by that release |

Only the five Rust library crates are published to crates.io. The CLI, C FFI
crate, and TypeScript N-API crate are built through their release workflows but
are not published as Rust crates.

## Upgrade Notes

- Go callers should always arrange `Close`, including on earlier errors. A
  failed close keeps the handle active and may be retried.
- TypeScript callers should use `await using` or call `close()` in a `finally` block
  so native ownership is released deterministically.
- A bounded `Wait`/`wait` timeout reports the current active state and does not
  terminate the child. The execution deadline, when configured at spawn, is a
  separate native policy.
- Go `Wait` rejects negative durations. Positive sub-millisecond durations are
  bounded waits rather than infinite waits.
- The existing `RunWithTimeout`, `terminateTree`, and `spawnInGroup` APIs keep
  their existing contracts.
- Managed spawn on Windows remains unavailable until a create-suspended Job
  assignment path can prove containment before first execution.

## Cargo Publication Order

Publish only after the exact `v0.2.4` tag is on `origin` and points at the
intended release commit.

1. `sysprims-core`
2. `sysprims-signal`
3. `sysprims-session`
4. `sysprims-proc`
5. `sysprims-timeout`

Confirm each predecessor is visible through `cargo info --registry crates-io`
before publishing the next dependent crate. Do not publish `sysprims-cli`,
`sysprims-ffi`, or `sysprims-ts-napi` to crates.io.
