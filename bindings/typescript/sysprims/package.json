{
  "name": "@3leaps/sysprims",
  "version": "0.1.5",
  "description": "GPL-free cross-platform process utilities (TypeScript bindings via C-ABI shared libs)",
  "license": "MIT OR Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "_lib/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node scripts/clean.js",
    "build": "tsc -p tsconfig.build.json",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run clean && npm run vendor:local && npm run build && npm run build:test && node scripts/run-tests.js",
    "test:ci": "npm run clean && npm run build && npm run build:test && node scripts/run-tests.js",
    "vendor:local": "node scripts/vendor-local-libs.js",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "dependencies": {
    "koffi": "^2.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
